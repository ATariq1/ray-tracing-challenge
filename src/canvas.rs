
use crate::color;

struct Canvas {
    x: i32,
    y: i32,
    canvas: Vec<color::Color>;
}

impl Canvas {
    pub fn new(x:i32, y:i32) -> Canvas {

        
    }

    pub fn get_pixel(x:i32,y:i32) -> color::Color {


    }
}

